"use client";
import { useState } from "react";
export function OpsConciergeCapacity({ concierges }: { concierges: { email: string; identity_id: string }[] }) { const [message, setMessage] = useState<string | null>(null); return <section className="ops-finance"><header><p className="eyebrow">Operação Concierge</p><h2>Capacidade de carteira</h2><p className="summary">Padrão: 100 membros ativos. A distribuição automática só usa quem tem vaga.</p></header><form className="auth-form" onSubmit={async (event) => { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/ops/concierge-capacity", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ identityId: form.get("identityId"), capacity: form.get("capacity") }) }); setMessage(response.ok ? "Capacidade atualizada e auditada." : "Não foi possível atualizar a capacidade."); }}><label>Concierge<select name="identityId">{concierges.map((concierge) => <option key={concierge.identity_id} value={concierge.identity_id}>{concierge.email}</option>)}</select></label><label>Máximo de membros ativos<input name="capacity" type="number" defaultValue={100} min={1} max={1000} required /></label><button>Salvar capacidade</button></form>{message && <p className="feedback">{message}</p>}</section>; }
