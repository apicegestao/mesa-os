import { NextResponse } from "next/server";
import { z } from "zod";
import { createSupabaseServerClient } from "@/shared/infrastructure/supabase/server";
export async function POST(request: Request) { const parsed = z.object({ identityId: z.string().uuid(), capacity: z.coerce.number().int().min(1).max(1000) }).safeParse(await request.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "invalid_request" }, { status: 400 }); const supabase = await createSupabaseServerClient(); const { data: claims } = await supabase.auth.getClaims(); if (!claims?.claims?.sub) return NextResponse.json({ error: "forbidden" }, { status: 403 }); const rpc = supabase.rpc as unknown as (name: string, args: Record<string, unknown>) => Promise<{ error: unknown }>; const { error } = await rpc("set_internal_concierge_capacity", { target_identity_id: parsed.data.identityId, target_capacity: parsed.data.capacity }); return error ? NextResponse.json({ error: "unavailable" }, { status: 422 }) : NextResponse.json({ ok: true }); }
